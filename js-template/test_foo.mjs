import {describe, it} from '../common/exams/lib/exams';
import assert from 'assert';
// import { Bar } from './foo';

describe("foo.Bar", () => {
    it("baz", () => {
        assert.equal(1 + 2, 3);
    });
    it("gaw", () => {
        assert.equal(true, false);
    });
});

